# Direct dependencies only, pinned.
#
# Transitive dependencies are deliberately left to pip's resolver. An earlier
# version of this file pinned the entire transitive tree, which collided with
# supabase's own requirements and had to be partially unpinned - leaving a mix
# of pinned and floating versions that was worse than either approach alone.
#
# To upgrade: change a version here, reinstall, run the app, and commit only
# after it works. Do not paste `pip freeze` back into this file.

# --- UI and app framework ---
streamlit==1.58.0

# --- AI ---
anthropic==0.109.1

# --- Database and auth ---
psycopg2-binary==2.9.12
supabase==2.31.0

# --- Resume and job-description parsing ---
pdfplumber==0.11.9
python-docx==1.2.0
pillow==12.2.0

# --- Data handling ---
pandas==3.0.3

# --- Configuration ---
python-dotenv==1.2.2
